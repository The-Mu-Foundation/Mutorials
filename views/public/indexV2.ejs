<%- contentFor('head') %>
<script src="https://cdn.jsdelivr.net/npm/typed.js@2.0.11"></script>
<script defer>
  const $ = document.querySelector.bind(document);

  document.addEventListener('DOMContentLoaded', () => {
    const typedOptions = {
      strings: ['free', 'challenging', 'adaptive', 'open-source'],
      typeSpeed: 40,
      backSpeed: 40,
      backDelay: 1500,
      startDelay: 500,
      loop: true,
      preStringTyped: (pos, self) => {
        $("#typed-a").innerText = (pos > 1) ? "an" : "a";
      },
    };
    const typed = new Typed("#typed-span", typedOptions);
    typed.start();
  });
</script>

<script src="https://cdnjs.cloudflare.com/ajax/libs/countup.js/2.7.0/countUp.umd.min.js"></script>


<%- contentFor('preMain') %>
<%- include('../partials/navigationV2') %>


<%- contentFor('main') %>
<%# Hero %>
<div class="w-full max-w-4xl m-0 sm:mt-36 sm:mb-40">
  <div class="flex justify-between w-full">
    <div class="flex flex-col items-start w-7/12 mr-6">
      <h1 class="mb-4 whitespace-nowrap">
        Mutorials is <span id="typed-a">a</span><br>
        <span class="font-extrabold bg-clip-text text-transparent bg-gradient-to-r from-blue-600 to-indigo-600" id="typed-span"></span><br>
        science trainer.
      </h1>
      <p class="mb-2 mr-4">
        Practice <b>thousands</b> of science problems from honors curriculum to national competition level—all with explanations.
      </p>
      <a class="btn btn-outline" href="/signup">
        Get started
        <i class="fas fa-arrow-right"></i>
      </a>
    </div>

    <div class="relative flex items-center w-5/12 overflow-visible -z-10">
      <%# Terrible hack to center oversized image in a div %>
      <%# https://stackoverflow.com/questions/14562457/center-oversized-image-in-div %>
      <img
        class="absolute opacity-20 sm:opacity-100 w-[600px] top-[-9999px] bottom-[-9999px] left-[-9999px] right-[-9999px] m-auto max-w-none"
        src="https://mutorials03.azureedge.net/images/icons/Icon_Fancy.svg"
      >
    </div>
  </div>
</div>

<%# "Wide coverage" -> subjects %>
<div class="flex flex-col items-center max-w-4xl my-10">
  <h2 class="mb-2">Wide coverage</h2>
  <h3 class="mb-4">Four core subjects with 60 distinct units.</h3>

  <div class="flex gap-4">
    <% for (let [subject, unitCount, image] of [
      ['Physics', 15, 'https://cdn.mutorials.org/images/icons/Physics_Main.svg'],
      ['Chemistry', 15, 'https://cdn.mutorials.org/images/icons/Chem_Main.svg'],
      ['Biology', 15, 'https://cdn.mutorials.org/images/icons/Bio_Main.svg'],
      ['Earth & Space', 15, 'https://images.unsplash.com/photo-1634176866089-b633f4aec882'],
    ]) {%>
      <div class="flex flex-col items-center w-1/4">
        <img class="mb-2 object-cover rounded-md" src="<%= image %>" draggable="false">
        <h3 class="mb-0 text-sm sm:text-normal text-center font-bold"><%= subject %></h3>
        <p><%= unitCount %> units</p>
      </div>
    <% } %>
  </div>
</div>

<%# Stats %>
<div class="flex justify-center max-w-4xl my-10 gap-4">
  <% for (let [quantity, label, id] of [
    [3310, 'questions with solutions', 'question-count'],
    [412, 'unique tags', 'tag-count'],
    [29767, 'total solves', 'solve-count']
  ]) { %>
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        var c = new countUp.CountUp(
          `sitestat-<%= id %>`,
          '<%= quantity %>',
          { enableScrollSpy: true, scrollSpyOnce: true, scrollSpyDelay: 200 }
        );
        c.start();
      })
    </script>
    <div class="flex flex-col items-center w-48">
      <h1 class="font-extrabold text-blue-600" id="sitestat-<%= id %>"><%= quantity %></h1>
      <p><%= label %></p>
    </div>
  <% } %>
</div>

<%# USABO %>
<div class="flex flex-col items-center w-screen ml-3 px-6 py-10 bg-blue-800 text-white">
  <p class="mb-8 px-4 py-2 bg-gradient-to-r from-blue-700 to-blue-500 font-bold rounded-full">NEW!</p>
  <h1 class="mb-2">USABO Training</h1>
  <h3 class="mb-4 text-center">The USA Biology Olympiad is very cool. 👍 We do USABO problems too.</h3>
  <div class="w-96">
    <img class="rounded-xl aspect-video object-cover" src="https://images.unsplash.com/photo-1576086213369-97a306d36557">
  </div>
</div>

<%# Pre-footer "get started" and "join us" %>
<div class="flex flex-col items-center max-w-4xl py-40">  
  <div class="w-24 p-4 mb-4 bg-neutral-100 rounded">
    <img src="https://cdn.mutorials.org/images/icons/Icon_Main.svg">
  </div>
  <h1 class="mb-4 text-center">Get started.</h1>
  <div class="flex items-center gap-4">
    <a class="btn btn-primary" href="/signup">Start training!</a>
    <a class="btn btn-outline" href="/whoWeAre">Join the team</a>
  </div>
</div>

<%# Footer %>
<%- include('../partials/footerV2.ejs') %>
